name: aphasia_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  camera: ^0.10.3
  flutter:
    sdk: flutter
  flutter_tts: ^3.6.3
  permission_handler: ^10.2.0
  throttling: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
